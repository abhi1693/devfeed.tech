# DevFeed brand assets

- `packages/theme/assets/devfeed-mark.png`: standalone transparent icon, used in both app headers, the admin login screen, and browser icon metadata.
- `packages/theme/assets/devfeed-wordmark.png`: transparent horizontal lockup for light backgrounds, with the lowercase `devfeed.` name.

The mark combines curly braces with three feed lines. The original artwork uses teal `#08796b` and dark evergreen `#20332d`. Both UIs pair the mark with theme-aware text so the name stays readable in light and dark mode. These are generated raster masters, not SVG files. Preserve transparency and aspect ratio. The header uses Next image optimization to serve a small rendition of the master.

Generated with the built-in image-generation tool from the existing brace icon. No API key or fallback CLI was used.

## Mark prompt

Use case: logo-brand. Asset: production standalone DevFeed logo icon. Reference image is the current thin teal curly braces { } mark; evolve it into a polished distinctive brand symbol while retaining unmistakable curly braces as the core idea. Create one centered icon, no text, no presentation sheet. Design two confidently weighted custom geometric curly braces with soft corners and clean optical symmetry; between them place three small horizontal feed strokes, vertically stacked with subtly different lengths, creating a compact developer-news symbol. Simplify aggressively so it remains recognizable at 24px. Single flat deep teal #08796b, no gradients, shadows, 3D effects, outlines around the whole icon or decorative extras. Transparent alpha background, square 1024x1024 canvas, icon fills about 78 percent of width and height with generous equal clear space. Crisp precise vector-like edges. This is a finished usable logo asset, not a mockup. Keep it ownable and calm, editorial and developer focused.

## Wordmark prompt

Use case: logo-brand. Make a finished horizontal DevFeed wordmark asset on a truly transparent alpha background. Use the input as the exact logo symbol: thick rounded teal curly braces enclosing three horizontal feed strokes. Preserve its structure and color. Place that symbol on the left and the exact lowercase word 'devfeed.' on the right. Text is bold geometric sans serif with open counters, confident professional editorial technology brand typography, dark evergreen #20332d letters and a teal #08796b terminal dot. The symbol is teal #08796b. Balanced optical alignment and thoughtful spacing. Horizontal composition with icon approximately equal to the wordmark cap height plus descenders; generous gap. Tight useful canvas around the single horizontal logo with a modest even safe margin, about 4:1 overall aspect ratio. Flat crisp vector-like design with no gradients, texture, shadows, mockups, backgrounds, extra text, or presentation panels. Output just one clean transparent logo lockup asset.
