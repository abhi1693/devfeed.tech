## What changes and why?

<!-- Describe the problem and the resulting behavior. For a bug, a short before/after example helps. -->

## Related issues

<!-- Use "Closes #123" only when this PR fully resolves that issue. Otherwise use "Related to #123", or remove this section. -->

## Validation

<!-- List the checks you ran and their results. Include screenshots for visible UI changes.
If a relevant check wasn't run, say why. For documentation-only changes, checking links and rendering is enough locally.
See https://github.com/abhi1693/devfeed.tech/blob/master/docs/ci.md for commands and the full CI suite.
-->

## Rollout notes

<!-- Remove this section if it doesn't apply.
Call out API/configuration changes, migrations, compatibility risks, and any rollback limits.
For dependency, runtime, Docker or CI changes, note relevant security results and AMD64/ARM64 verification.
For releases, identify the version and verified image manifest; never replace an existing release tag.
-->

## Ready for review

- [ ] The description explains the change and the validation results, including any gaps.
- [ ] Relevant documentation and regression tests are updated, or the description explains why they aren't needed.

<!-- Before merging, the current commit must pass CI required and CodeQL rules, with review conversations resolved.
CI covers unit/integration tests, frontend lint/tests/build, security checks, and runtime image scans/smoke tests on both architectures.
-->
